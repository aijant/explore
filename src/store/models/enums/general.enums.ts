export enum Roles {
  ADMIN = 'admin',
  OPERATOR = 'operator',
  USER = 'user',
  SUPER_ADMIN = 'superAdmin'
}

export enum Status {
  FINISHED = 'Finished',
  IN_PROCESS = 'InProcess',
  FOR_INFO = 'ForInformation'
}
